<UserControl
    x:Class="Brainrot.UI.TaskbarWidgetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="360"
    d:DesignWidth="280">

    <Border CornerRadius="14" Padding="16" BorderThickness="1" Background="#F0252A35" BorderBrush="#443F4A58">
        <StackPanel Spacing="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <TextBlock Text="Brainrot" FontSize="16" FontWeight="SemiBold" />
                <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
                    <TextBlock Text="🧠" />
                </StackPanel>
            </StackPanel>

            <Border Background="#1AFFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1" CornerRadius="10" Padding="10">
                <StackPanel Spacing="6">
                    <TextBlock x:Name="Title" Text="Locked in" FontSize="18" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <Border Width="40" Height="40" CornerRadius="10" Background="#22FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1">
                            <TextBlock x:Name="Glyph" Text="🙂" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22" />
                        </Border>
                        <StackPanel>
                            <TextBlock x:Name="Percent" Text="100% focus" FontSize="14" />
                            <ProgressBar x:Name="Bar" Minimum="0" Maximum="100" Height="8" CornerRadius="6" Width="160" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Spacing="6">
                <TextBlock Text="Today" FontSize="14" FontWeight="SemiBold" />
                <StackPanel Spacing="4">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Focus" Width="80" />
                        <TextBlock x:Name="FocusText" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Neutral" Width="80" />
                        <TextBlock x:Name="NeutralText" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Rot" Width="80" />
                        <TextBlock x:Name="RotText" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
