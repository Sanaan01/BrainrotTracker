<UserControl
    x:Class="Brainrot.UI.TaskbarWidgetCompact"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="44" d:DesignWidth="70"
    Background="Transparent">

    <Grid x:Name="RootBorder"
          Background="Transparent"
          PointerPressed="Root_PointerPressed"
          PointerMoved="Root_PointerMoved"
          PointerReleased="Root_PointerReleased"
          PointerCaptureLost="Root_PointerCaptureLost"
          PointerEntered="Root_PointerEntered"
          PointerExited="Root_PointerExited">
        
        <!-- Hover background with opacity animation -->
        <Border x:Name="HoverBorder" 
                Background="#20FFFFFF" 
                CornerRadius="4"
                Opacity="0"/>
        
        <!-- Content -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="0" Margin="6,4">
            <TextBlock x:Name="StateLabel" 
                       Text="Mixed" 
                       FontSize="12" 
                       FontWeight="Normal"
                       Foreground="#FFFFFFFF"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="FocusPercent" 
                       Text="58% focus" 
                       FontSize="10" 
                       Foreground="#80FFFFFF"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
